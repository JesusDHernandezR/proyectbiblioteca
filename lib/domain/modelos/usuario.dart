class Usuario {
  var nombre;
  var apellido;
  var tipoDeUsuario;
  var correo;
  var contrasena;
  Usuario(
      {this.nombre,
      this.apellido,
      this.tipoDeUsuario,
      this.correo,
      this.contrasena});
}
